jdbc:
  driverClassName: com.mysql.jdbc.Driver
  url: jdbc:mysql://127.0.0.1:3306/springboot_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
  username: root
  password: root

# 激活配置文件；需要指定其他的配置文件名称

spring:
  profiles:
    active: abc
